{
  "name": "device-see",
  "version": "0.1.0",
  "description": "Stream a physical iOS device screen in the browser (npx device-see)",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hly-AI/serve-sim.git",
    "directory": "packages/serve-device"
  },
  "bin": {
    "device-see": "dist/device-see.js",
    "serve-device": "dist/device-see.js"
  },
  "files": [
    "dist/device-see.js",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "bun ./build.ts",
    "test": "bun test src",
    "prepublishOnly": "bun ./build.ts"
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "commander": "^14.0.1",
    "serve-runtime": "workspace:*",
    "typescript": "^5.7.0"
  },
  "keywords": [
    "ios",
    "device",
    "screenshot",
    "mjpeg",
    "devicectl",
    "serve-device"
  ]
}
