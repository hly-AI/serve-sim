# serve-device skill

Agent skill for physical iOS devices via `npx serve-device`. See [SKILL.md](./SKILL.md).

Scripts:

- `scripts/check-prereqs.sh`
- `scripts/ensure-running.sh` (calls **serve-device only**, never serve-sim)
